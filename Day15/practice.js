/*
function salary(a, b) {
    console.log("Total Salary:");
    return a + b;
}

let expense = function(c, d) {
    console.log("Total Expenditure:");
    return c + d;
}

let a = 30000;  // Example: base salary
let b = 10000;  // Example: bonus
let c = 15000;  // Example: rent
let d = 5000;   // Example: bills

let x = salary(a, b);       // Total Salary
let y = expense(c, d);      // Total Expenditure

let savings = (l, m) => l - m;
let result = savings(x, y);

console.log("Total Savings:");
console.log(result);
*/

let arr=[1,27,6,8,9,0];
str=arr.slice(1,3);
console.log(arr);
console.log(str);